# open-grok provider platform execution state

This file is mutable execution state. It is not the goal objective, fact contract, or approved implementation authority.

## Authority status

- Interview provenance exists and is preserved.
- `facts.md` and `facts.meta.json` contain the final 39-fact provider-platform and unified-session-repository authority. The earlier completed 31-fact review is preserved under `facts-history/`; direct final amendments resolved the review annotations without another browser interview.
- The prior planning-agent output remains discarded because it was anchored to rejected per-session SQLite, permanent JSONL, and fragmented-catalog assumptions. Current authority instead establishes one local-or-remote `open-grokd`, bounded memory-hot state, progress-before-background-durability semantics with explicit watermarks, a benchmark-gated SQLite starting engine, exact node cursors, transport-neutral attachment, optional remote repository placement behind the daemon, and two non-destructive provider-context recovery paths.
- `plan.md` is approved authority. The native gate returned an acceptable plan with comments, the operator delegated direct amendments, structured `AskUserQuestion` interviews resolved durability, recovery, performance, remote attachment, product feel, default permission posture, and red lines, and successive focused reviews passed after remediation without another Plannotator round.
- `goal.md` now contains the self-contained invariant objective. Its final artifact-only review identified completeness omissions rather than architectural contradictions; credential rotation, capability gating, catalog provenance, auth fallback, login UX, tool documentation, disconnect continuation, and remaining accepted-fact details were restored directly.

## Preserved concurrent work

- The original xAI publication is preserved on a protected archive branch.
- The replacement xAI snapshot has been audited but is not currently integrated; the direct import was reverted so a reviewed synchronization path can be decided.
- CI, upstream synchronization, decision-record, Cargo test-support, lint, and documentation drafts remain preserved on `ci/upstream-sync-gates`.
- Earlier goal and provenance artifacts remain preserved in the repository stash created before the branch workflow correction.
- The application repository has transferred to `open-grok/open-grok` after explicit operator approval and fresh preflight. Postflight verified `main` at `101aa129858f5a0af06ee153baec2819b7d9f9b1`, the protected archive at `c1b5909ec707c069f1d21a93917af044e71da0d7`, repo-level `main` protection with admin enforcement and no force-push/deletion, unchanged 51-path local dirt, updated `origin`, updated public links, and verified chezmoi/Mani inventory; `upstream` remains `xai-org/grok-build`.

## Resume rule

Resume the first incomplete work in Phase 0/1 of the approved plan: finish path-by-path classification of the preserved dirty foundation, validate the invariant goal package, and deliver coherent public documentation/governance, Cargo/test-support, and CI/upstream-sync checkpoints without discarding or bulk-replacing preserved work.
