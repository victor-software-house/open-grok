# open-grok provider platform goal

Goal setup is in discovery. No objective, architecture, or implementation plan is approved yet.

This directory will contain the reviewed provenance package:

- `interview.json` and `interview-result.json`
- `facts-review.json`, `facts-result.json`, `facts.md`, and `facts.meta.json`
- `plan.md`
- `goal.md`

The final goal must be self-contained and invariant while its underlying evidence references can evolve through explicit pinned updates. It will not use a mutable status document as authority.

Prerequisite: complete and review [`../../docs/architecture/`](../../docs/architecture/).
