# OpenAI Codex evidence refresh `2edad72`

## Provenance

| Field | Value |
|---|---|
| Upstream | [`openai/codex`](https://github.com/openai/codex) |
| Previous evidence pin | [`78d4a563f9ecaf5c331bfa4ae7332885a1d9e2f6`](https://github.com/openai/codex/tree/78d4a563f9ecaf5c331bfa4ae7332885a1d9e2f6) |
| New evidence pin | [`2edad72de3e4fb12a7519027d5eb3cbda45eea6c`](https://github.com/openai/codex/tree/2edad72de3e4fb12a7519027d5eb3cbda45eea6c) |
| Git relationship | New revision directly descends from the old revision by one commit |
| Commit | `Track prompt cache write token usage (#33454)` |
| Integration type | Evidence-pin advancement only; no Codex code imported |
| User-visible change | no |

## Review

The change adds a distinct `cache_write_input_tokens` usage counter sourced from OpenAI Responses `input_tokens_details.cache_write_tokens`.

It propagates the optional accounting field through protocol types, cumulative usage, per-turn and goal accounting, observability, generated schemas, SDK types, and tests. Missing fields default to zero for compatibility.

The reviewed delta does **not** change:

- Codex authentication or credential flows;
- provider routing;
- prompt-cache keying or invalidation behavior;
- previous-response chaining;
- session storage, resume, compaction, or conversation history.

`open-grok` cites Codex as a Rust reference for Codex authentication. Because the one-commit delta does not affect that behavior, the evidence pin advanced without changing the associated architecture claims.

## Verification

Direct GitHub checks verified the `openai/codex` repository, resolved its default-branch head to `2edad72…`, inspected the named commit, and compared `78d4a56…` with `2edad72…`. The comparison confirmed a one-commit linear advancement. No local runtime or code tests ran because no Codex code was imported.

## Decision

**Advance the Codex source-ledger pin to `2edad72…`.**

No source import, user-facing changelog entry beyond provenance, or provider-platform requirement is derived from the new telemetry counter.
